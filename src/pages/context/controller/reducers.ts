import * as t from './type';
import { AnyAction } from 'redux'
const initialState = {};

function contextReducer (state = initialState, action: AnyAction) {
    return state
}

export default contextReducer;