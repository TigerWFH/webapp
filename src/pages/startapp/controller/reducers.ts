import * as t from './type'
import {AnyAction} from 'redux'
const initialState = {};

function startappReducer (state = initialState, action: AnyAction) {
    return state
}

export default startappReducer;